@model ShipLine.Models.RouteModel

@{
    ViewData["Title"] = "CreateRoute";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>CreateRoute</h1>

<h4>RouteModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @*<div class="form-group">
                <label asp-for="RouteId" class="control-label"></label>
                <input asp-for="RouteId" class="form-control" />
                <span asp-validation-for="RouteId" class="text-danger"></span>
            </div>*@
            <div class="form-group">
                <label asp-for="SourcePortId" class="control-label">Source Port</label>
                @Html.DropDownList("SourcePortId", new SelectList(ViewBag.PortList, "Value", "Text"), "", new{@class="form-control"})
                <span asp-validation-for="SourcePortId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="DestinationPortId" class="control-label">Destination Port</label>
                @Html.DropDownList("DestinationPortId", new SelectList(ViewBag.PortList, "Value", "Text"), "", new{@class="form-control"})
                <span asp-validation-for="DestinationPortId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Name" class="control-label">Route Name</label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
